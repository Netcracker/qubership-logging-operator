# Stage 2 migrate prompts

Minimal product wording for executor subagents. Do not include rubric or gate tables here — graders use `grading/expectations.json`.

| File | Eval |
| ---- | ---- |
| `migrate-go-log-exporter.md` | `go-log-exporter` |
| `migrate-java-dbaas-monorepo.md` | `java-dbaas-monorepo` |

Both assume **post-enable** worktrees (JSON envelope already configured).
