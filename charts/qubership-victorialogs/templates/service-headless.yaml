{{- if .Values.victorialogs.install }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "victorialogs.headlessServiceName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "victorialogs.serviceLabels" (dict "ctx" . "role" "headless") | nindent 4 }}
  {{- with .Values.victorialogs.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: ClusterIP
  clusterIP: None
  ports:
    - name: http
      port: {{ .Values.victorialogs.service.port }}
      protocol: TCP
      targetPort: http
  selector:
    {{- include "victorialogs.selectorLabels" . | nindent 4 }}
{{- end }}
