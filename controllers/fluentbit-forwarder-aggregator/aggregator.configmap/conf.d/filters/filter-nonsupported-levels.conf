[FILTER]
    Name              lua
    Match             *
    script            /fluent-bit/etc/update_level_syslog.lua
    call              update_level


# Restore the application source level only when normalization did not create one
[FILTER]
    Name         modify
    Match        *
    Rename       parsed_source_level source_level
