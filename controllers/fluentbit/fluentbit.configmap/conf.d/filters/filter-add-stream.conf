{{- if .Values.Fluentbit.Output }}
{{- if or (and .Values.Fluentbit.Output.Loki .Values.Fluentbit.Output.Loki.Enabled) (and .Values.Fluentbit.Output.Http .Values.Fluentbit.Output.Http.Enabled) (and .Values.Fluentbit.Output.Otel .Values.Fluentbit.Output.Otel.Enabled) }}
[FILTER]
    Name   record_modifier
    Match_regex  (pods|klog).*
    Record stream container
[FILTER]
    Name   record_modifier
    Match  audit*
    Record stream audit
[FILTER]
    Name   record_modifier
    Match  system*
    Record stream system
[FILTER]
    Name   modify
    Match_regex  (pods|klog).*
    Condition Key_value_matches nc_audit_label true
    Set    stream audit
{{- end }}
{{- end }}
