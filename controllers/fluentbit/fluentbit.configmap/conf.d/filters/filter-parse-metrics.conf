{{- if .Values.fluentbit.output.prometheus.enabled }}
[FILTER]
    Name            log_to_metrics
    Match           *
    Tag             parse_error_total
    Metric_Mode     counter
    Metric_Name     parse_error_total
    Metric_Description  Total number of parse errors
    Label_Field     pod
    Label_Field     container
{{- end }}
