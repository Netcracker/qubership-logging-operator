{{- if and .Values.fluentbit.output.prometheus.enabled .Values.fluentbit.output.prometheus.port .Values.fluentbit.output.prometheus.host }}
[OUTPUT]
    Name prometheus_exporter
    Match *
    Host {{ .Values.fluentbit.output.prometheus.host }}
    Port {{ .Values.fluentbit.output.prometheus.port }}
{{- end }}
